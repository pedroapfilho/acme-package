# oxlint is type-aware here, so without workspace .d.ts on disk it reports
# unresolved cross-package types as lint violations. Turbo replays this from
# cache in under a second.
pnpm build:packages --output-logs=errors-only
pnpm lint-staged
